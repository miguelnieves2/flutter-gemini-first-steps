name: gemini_first_steps
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  go_router: ^14.2.1
  google_generative_ai: ^0.4.3

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_riverpod: ^2.5.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - .env
